import {createSlice} from "@reduxjs/toolkit"

const initialState = ("");

const username = createSlice({
    name:"username",
    initialState,
    reducers:{
        username:(state,action)=>{

        }
    }
});